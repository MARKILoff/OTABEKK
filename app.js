let options = {
    strings: ["ssss"],
    typeSpeed: 60,
    startDelay: 2000,
    backSpeed: 50,
    backDeplay: 50,
    loop: true,
    loopCount: Infinity,
};

let typed = new Typed('.element', options);